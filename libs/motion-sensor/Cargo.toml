[package]
name = "motion-sensor"
version = "0.1.0"
authors = ["Ian McIntyre <ianpmcintyre@gmail.com>"]
edition = "2018"

[dependencies.serde]
optional = true
version = "1.0"
default-features = false
features = ["derive"]

[features]
use-serde = ["serde"]